// const fs = require('fs');
// const input = fs.readFileSync('/dev/stdin').toString().split(' ').map(value => +value);

const text = 'joonas';
const input = text.toString().split(' ');

console.log(input);
console.log(input + '??!');